---
allowed-tools: Bash(source:*), Bash(git:*), Bash(echo:*), Bash(pbcopy:*)
description: Get Vercel preview URL with bypass token for current branch
argument-hint: [branch-name]
---

# Get Vercel Preview URL with Bypass Token

Get the complete preview URL for the current git branch with the Vercel protection bypass token automatically appended.

Execute the preview URL script and copy to clipboard:

!`cd /Users/oliver/Documents/internal/cl-website-jul25 && ./get-preview-url.sh`
